import React from 'react';
import { Modal, View, Text, StyleSheet, TouchableOpacity, ScrollView } from 'react-native';
import { Ionicons } from '@expo/vector-icons'; // Expo-specific icon import

const InfoModal = ({ visible, onClose }) => {
  return (
    <Modal
      animationType="slide"
      transparent={true}
      visible={visible}
      onRequestClose={onClose}
    >
      <View style={styles.centeredView}>
        <View style={styles.modalView}>
          <Text style={styles.modalTitle}>How to Track</Text>
          <ScrollView style={styles.content}>
            [cite_start]{/* [cite: 28, 29] - Instructions matching requirements */}
            <Text style={styles.bulletPoint}>• Get into a comfortable position, preferably lying on your side.</Text>
            <Text style={styles.bulletPoint}>• Tap the screen every time you feel a kick, flutter, or roll.</Text>
            <Text style={styles.bulletPoint}>• The goal is to reach 10 movements.</Text>
            <Text style={styles.bulletPoint}>• Stop the timer once you reach 10 kicks.</Text>
          </ScrollView>
          <TouchableOpacity
            style={styles.closeButton}
            onPress={onClose}
          >
            <Text style={styles.textStyle}>Close</Text>
          </TouchableOpacity>
        </View>
      </View>
    </Modal>
  );
};

const styles = StyleSheet.create({
  centeredView: {
    flex: 1,
    justifyContent: 'flex-end',
    backgroundColor: 'rgba(0,0,0,0.5)',
  },
  modalView: {
    backgroundColor: 'white',
    borderTopLeftRadius: 20,
    borderTopRightRadius: 20,
    padding: 35,
    maxHeight: '50%',
    shadowColor: '#000',
    shadowOffset: { width: 0, height: -2 },
    shadowOpacity: 0.25,
    shadowRadius: 4,
    elevation: 5,
  },
  modalTitle: {
    fontSize: 22,
    fontWeight: 'bold',
    marginBottom: 15,
    textAlign: 'center',
    color: '#333',
  },
  bulletPoint: {
    fontSize: 16,
    marginBottom: 12,
    lineHeight: 24,
    color: '#444',
  },
  closeButton: {
    backgroundColor: '#FF6B6B',
    borderRadius: 25,
    padding: 12,
    marginTop: 20,
    alignSelf: 'center',
    width: '100%',
    alignItems: 'center',
  },
  textStyle: {
    color: 'white',
    fontWeight: 'bold',
    fontSize: 16,
  },
});

export default InfoModal;
